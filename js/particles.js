(()=>{const canvas=document.getElementById("particles");if(!canvas)return;const ctx=canvas.getContext("2d");let ps=[],w=0,h=0,dpr=1;
function p(){return{x:Math.random()*w,y:Math.random()*h,size:Math.random()*1.5+.4,speed:Math.random()*.25+.08,drift:(Math.random()-.5)*.12,alpha:Math.random()*.38+.08}}
function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=Math.round(w*dpr);canvas.height=Math.round(h*dpr);canvas.style.width=w+"px";canvas.style.height=h+"px";ctx.setTransform(dpr,0,0,dpr,0,0);ps=Array.from({length:Math.min(80,Math.max(28,Math.floor(w/8)))},p)}
function draw(){ctx.clearRect(0,0,w,h);for(const x of ps){x.y-=x.speed;x.x+=x.drift;if(x.y<-5){x.y=h+5;x.x=Math.random()*w}if(x.x<-5)x.x=w+5;if(x.x>w+5)x.x=-5;ctx.beginPath();ctx.fillStyle=`rgba(214,190,145,${x.alpha})`;ctx.arc(x.x,x.y,x.size,0,Math.PI*2);ctx.fill()}requestAnimationFrame(draw)}addEventListener("resize",resize,{passive:true});resize();draw()})();
